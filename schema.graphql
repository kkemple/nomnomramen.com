type Query {
  products: ProductConnection!
}

type ProductConnection {
  hasMore: Boolean
  items: [Product!]!
}

type Product {
  id: ID!
  name: String!
  description: String
  images: [String!]!
}
